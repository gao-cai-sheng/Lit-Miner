# Technical Decisions & Open Issues

本文档记录Lit-Miner项目的关键技术决策、设计权衡、待解决问题和质量保障策略。

**最后更新**: 2025-12-20 (v2.0.1)  
**维护者**: 开发团队  
**实施状态**: 🟢 已实现 | 🟡 部分实现 | 🔴 待实现

---

## 🎯 核心技术决策

### 1. AI查询扩展可靠性评估

#### ❓ 核心问题
**DeepSeek API翻译医学术语的可靠性如何保证？**

这是AI驱动系统的根本性问题，涉及：
- 医学术语翻译准确性
- 同义词覆盖完整性
- 缩写识别正确性
- 跨语言一致性

#### 📊 评估方法

##### 方法1：黄金标准对照（Gold Standard Benchmark）

**建立测试集**：
```python
# test_data/medical_terms_benchmark.json
{
  "neuroscience": [
    {
      "chinese": "阿尔茨海默病",
      "gold_standard": ["Alzheimer's disease", "AD", "Alzheimer disease"],
      "source": "MeSH Database"
    },
    {
      "chinese": "帕金森病",
      "gold_standard": ["Parkinson's disease", "PD", "Parkinsonism"],
      "source": "NIH Medical Dictionary"
    }
  ],
  "cardiology": [...],
  "oncology": [...]
}
```

**评估指标**：
```python
def evaluate_expansion_quality(test_set):
    metrics = {
        "term_coverage": 0,      # 术语覆盖率
        "synonym_recall": 0,     # 同义词召回率
        "abbreviation_recall": 0, # 缩写召回率
        "false_positive_rate": 0  # 误报率
    }
    
    for test_case in test_set:
        ai_result = expand_query(test_case["chinese"])
        gold_terms = test_case["gold_standard"]
        
        # 计算包含了多少标准术语
        coverage = len(set(gold_terms) & set(extract_terms(ai_result))) / len(gold_terms)
        metrics["term_coverage"] += coverage
    
    return metrics
```

**目标指标**：
- ✅ 术语覆盖率 ≥ 90%（主要术语必须包含）
- ✅ 同义词召回 ≥ 80%（常用同义词）
- ✅ 误报率 ≤ 5%（错误术语）

##### 方法2：交叉验证（Cross-Validation）

**与权威数据库对比**：
```python
def validate_against_mesh():
    """
    对比MeSH（Medical Subject Headings）术语库
    MeSH是NIH维护的权威医学术语标准
    """
    mesh_api = "https://id.nlm.nih.gov/mesh/"
    
    user_query = "阿尔茨海默病"
    ai_expansion = expand_query(user_query)
    
    # 查询MeSH数据库
    mesh_terms = query_mesh_database("Alzheimer")
    
    # 计算重合度
    overlap = calculate_overlap(ai_expansion, mesh_terms)
    
    if overlap < 0.7:  # 低于70%重合度
        log_warning(f"Low MeSH overlap for: {user_query}")
```

**数据源**：
- MeSH (Medical Subject Headings) - NIH官方
- UMLS (Unified Medical Language System)
- SNOMED CT（临床术语系统）

##### 方法3：专家审核机制

**人工审核流程**：
```python
def flag_for_review(query, expansion, confidence):
    """
    标记需要专家审核的扩展结果
    """
    review_needed = False
    
    # 触发审核条件
    if confidence < 0.8:  # AI置信度低
        review_needed = True
    if is_rare_disease(query):  # 罕见病
        review_needed = True
    if contains_new_drug_name(query):  # 新药物
        review_needed = True
    
    if review_needed:
        save_to_review_queue({
            "query": query,
            "expansion": expansion,
            "confidence": confidence,
            "requires_expert": "medical_terminology"
        })
```

**专家反馈循环**：
```
AI生成扩展 → 专家审核 → 标记正确/错误 → 反馈训练数据
```

##### 方法4：A/B测试（实际效果检验）

**对比实验设计**：
```python
# 组A: AI扩展
results_ai = search_pubmed(ai_expanded_query)

# 组B: 静态词典
results_static = search_pubmed(static_expanded_query)

# 组C: 人工扩展（基准）
results_manual = search_pubmed(expert_query)

# 对比指标
metrics = {
    "top_10_overlap": overlap(results_ai[:10], results_manual[:10]),
    "high_impact_papers": count_high_impact(results_ai, results_manual),
    "user_satisfaction": survey_results()
}
```

**评估维度**：
- 检索结果质量（与专家检索的重合度）
- 顶刊论文覆盖率
- 用户满意度调查

##### 方法5：持续监控与异常检测

**自动质量监控**：
```python
class ExpansionQualityMonitor:
    def check_expansion(self, query, expansion):
        issues = []
        
        # 检查1: 空结果
        if not expansion or expansion == query:
            issues.append("EMPTY_EXPANSION")
        
        # 检查2: 过长（可能幻觉）
        if len(expansion) > 500:
            issues.append("TOO_LONG")
        
        # 检查3: 非医学术语（简单启发式）
        if not contains_medical_keywords(expansion):
            issues.append("NON_MEDICAL")
        
        # 检查4: 格式错误
        if not has_valid_boolean_operators(expansion):
            issues.append("INVALID_FORMAT")
        
        if issues:
            log_quality_issue(query, expansion, issues)
            trigger_fallback()
```

#### ✅ 当前保障措施

**已实现**：
1. **Fallback机制**: AI失败自动降级到静态词典
2. **温度控制**: Temperature=0.3保证一致性
3. **Prompt工程**: 明确要求医学术语+示例
4. **结果验证**: 检查长度、格式合法性

**待实现**（v2.1）：
- [ ] MeSH术语库集成
- [ ] 专家审核队列
- [ ] A/B测试框架
- [ ] 质量监控仪表板

#### 🎯 风险缓解策略

| 风险 | 影响 | 缓解措施 | 状态 |
|------|------|---------|------|
| **AI幻觉** | 生成错误术语 | Fallback+格式验证 | ✅ 已实现 |
| **术语遗漏** | 检索不全 | 增加PubMed检索量到200篇 | ✅ 已实现 |
| **成本失控** | API费用高 | 智能缓存机制 | ✅ 已实现 |
| **服务中断** | API不可用 | 静态词典fallback | ✅ 已实现 |
| **专业性不足** | 罕见病错误 | 专家审核队列 | ⏳ 计划中 |

---

### 2. 查询扩展缓存策略

#### ❓ 核心问题
**AI扩展结果是否应该持久化？如何维护？**

#### 方案对比

##### 方案A：仅内存缓存（当前实现）

**优点**：
- ✅ 简单高效
- ✅ 灵活性高（医学术语演进时自动更新）
- ✅ 无需维护数据库

**缺点**：
- ❌ 重启应用丢失缓存
- ❌ 无法统计分析使用情况
- ❌ 多实例无法共享缓存

**适用场景**: 
- 单机部署
- 用户量 < 100
- 快速迭代阶段

##### 方案B：JSON文件持久化

**数据结构**：
```json
{
  "阿尔茨海默病": {
    "expanded_query": "(\"Alzheimer's disease\" OR \"AD\")",
    "timestamp": "2025-12-19T23:00:00",
    "hit_count": 15,
    "last_used": "2025-12-19T23:44:00",
    "confidence": 0.95
  }
}
```

**更新策略**：
```python
# 场景1: 完全相同 → 仅更新统计
if cached_expansion == new_expansion:
    cache[query]["hit_count"] += 1
    cache[query]["last_used"] = now()

# 场景2: 结果不同 → 保留新版本，记录旧版本
else:
    cache[query]["expanded_query"] = new_expansion
    cache[query]["previous_versions"].append({
        "query": old_expansion,
        "deprecated_at": now()
    })
```

**维护策略**：
- 定期清理30天未使用的缓存
- 保留使用频率高的缓存（hit_count > 10）
- 手动审核低置信度的缓存

**优点**：
- ✅ 重启不丢失
- ✅ 可统计分析
- ✅ 人类可读/编辑

**缺点**：
- ⚠️ 需要定期维护
- ⚠️ 数据量大时性能下降

**推荐时机**: v2.1实现

##### 方案C：向量语义匹配

**架构**：
```python
# 使用ChromaDB存储查询向量
expansion_db = ChromaDB("query_expansions")

# 查询时语义匹配
similar_queries = expansion_db.query(
    query_texts=["阿尔茨海默病"],
    n_results=1
)

# 相似度 > 0.95 → 复用缓存
if similar_queries['distances'][0] < 0.05:
    return similar_queries['metadatas'][0]['expansion']
```

**优点**：
- ✅ 模糊匹配（"阿尔茨海默" ≈ "阿尔茨海默病"）
- ✅ 处理同义表述
- ✅ 可扩展性强

**缺点**：
- ❌ 复杂度高
- ❌ 需要embedding模型
- ❌ 可能误匹配

**推荐时机**: v3.0企业版

#### 🎯 推荐路线图

```
v2.0 (当前): 内存缓存
    ↓
v2.1: JSON持久化 + 清理策略
    ↓
v2.5: SQLite + 统计分析
    ↓
v3.0: 向量匹配 + 多实例共享
```

---

### 3. 期刊评分与检索深度

#### ❓ 核心问题
1. **检索深度100篇是否足够？**
2. **期刊评分依赖PubMed排序是否合理？**

#### 当前实现分析

**检索流程**：
```
PubMed返回（按relevance排序）
    ↓ 100篇（用户可调50-500）
SmartMiner二次筛选
    ↓
    ├─ 综述 (2篇)
    ├─ 顶刊最新 (4篇) ← 依赖期刊白名单
    └─ 高分研究 (4篇)
    ↓
最终输出: 8-10篇
```

#### 已识别的问题

**问题1**: 顶刊最新论文不足
- **现象**: "阿尔茨海默"查询仅选出2篇（而非4篇）
- **原因**: 2024年发表在顶刊的论文较少
- **影响**: 最终输出8篇而非10篇

**问题2**: PubMed排序的局限性
- PubMed关注"查询相关性"，不等于"科研质量"
- 可能将标题完美匹配但质量一般的论文排在前面
- 高质量论文可能排在101-200名

#### 解决方案

##### 短期（v2.1）
- [x] 提高默认检索深度: 20 → 200篇
- [x] 提高最大检索深度: 100 → 500篇
- [ ] 放宽时间窗口: 1年 → 3年

##### 中期（v2.5）
- [ ] 集成影响因子数据库
- [ ] 动态调整期刊评分
- [ ] 多策略排序（相关性+质量）

##### 长期（v3.0）
- [ ] 机器学习预测论文质量
- [ ] 用户反馈学习
- [ ] 个性化推荐

---

## 🔬 质量保障措施

### 已实施

#### 1. 多层Fallback机制
```
AI扩展（主） → 静态词典（备） → 通用扩展（保底）
```

#### 2. 二阶段筛选
```
PubMed相关性筛选 → SmartMiner质量筛选
```

#### 3. 智能缓存
- 相同查询 <1ms 返回
- 节省API成本
- 保证结果一致性

#### 4. 多领域期刊支持
- 50+顶级期刊
- 覆盖神经、心血管、肿瘤等

### 待实施（按优先级）

#### 🟢 高优先级（v2.1）
- [ ] **撤稿论文检测**: 过滤被撤回的论文
- [ ] **预印本标记**: bioRxiv/medRxiv降权
- [ ] **MeSH术语验证**: 对比NIH官方术语库
- [ ] **查询扩展日志**: 记录所有AI扩展结果供审核

#### 🟡 中优先级（v2.5）
- [ ] **影响因子集成**: 自动获取期刊IF评分
- [ ] **专家审核队列**: 人工审核低置信度扩展
- [ ] **A/B测试框架**: 对比不同扩展策略效果
- [ ] **质量监控面板**: 可视化质量指标

#### 🔵 低优先级（v3.0）
- [ ] **机器学习质量预测**: 预测论文未来引用数
- [ ] **用户反馈循环**: 基于用户标注优化
- [ ] **多模型ensemble**: 结合多个LLM结果

---

## 🤔 待决策问题

### 1. AI扩展置信度阈值

**问题**: 当AI返回低置信度结果时，是否应该fallback？

**选项**：
- A. 总是使用AI结果（当前）
- B. 置信度 < 0.8时fallback到静态词典
- C. 置信度 < 0.6时拒绝查询，要求用户重新表述

**影响因素**：
- 用户体验（fallback对用户透明）
- 查询成功率
- 结果质量

**建议**: 需要实际数据支持决策

---

### 2. 缓存过期策略

**问题**: AI扩展缓存应该保留多久？

**考虑因素**：
- 医学术语演进速度（新药物、新疗法）
- 缓存命中率 vs 准确性
- 存储成本

**当前**: 无过期（应用重启清空）

**建议**：
- 30天未使用 → 过期
- 使用频率高（>10次）→ 永久保留
- 新增术语（如新药）→ 7天过期

---

### 3. 多语言支持

**问题**: 是否支持中英文以外的语言？

**潜在需求**：
- 日语、韩语（亚洲研究者）
- 德语、法语（欧洲研究者）

**挑战**：
- AI翻译质量难以保证
- 缺乏验证数据
- PubMed主要是英文

**建议**: v3.0再考虑

---

### 4. 用户自定义评分权重

**问题**: 是否允许用户调整评分权重？

**当前固定权重**：
```python
journal: 40%
recency: 30%
citations: 10%
data_quality: 20%
```

**PRO功能候选**：
用户可以自定义为：
```python
journal: 20%   # 降低期刊权重
recency: 50%   # 强调最新研究
citations: 30% # 强调引用数
```

**利弊**：
- ✅ 个性化，满足不同研究需求
- ❌ 增加复杂度，可能误用

---

## 📋 技术债务

### 代码层面
- [ ] `smart_miner.py`中硬编码的筛选逻辑（2+4+4）
- [ ] UI中的magic numbers（slider步长50）
- [ ] 缺少单元测试（仅有集成测试）

### 架构层面
- [ ] Streamlit单体应用（扩展性受限）
- [ ] 配置分散在`config.py`和UI中
- [ ] 无用户系统（无法区分免费/PRO）

### 文档层面
- [ ] API文档缺失
- [ ] 缺少贡献者指南
- [ ] 代码注释不足

---

## 📚 参考资料

### 医学术语标准
- [MeSH (Medical Subject Headings)](https://www.nlm.nih.gov/mesh/)
- [UMLS (Unified Medical Language System)](https://www.nlm.nih.gov/research/umls/)
- [SNOMED CT](https://www.snomed.org/)

### 系统综述方法学
- [PRISMA Guidelines](http://www.prisma-statement.org/)
- [Cochrane Handbook](https://training.cochrane.org/handbook)

### AI质量评估
- [Evaluating Medical AI](https://www.nature.com/articles/s41591-020-0842-8)
- [DeepSeek Documentation](https://platform.deepseek.com/docs)

---

## 更新日志

| 日期 | 更新内容 | 提议人 |
|------|---------|--------|
| 2025-12-20 | **v2.0.1实施更新**：撤稿检测、预印本标记、IF集成已完成 | 开发团队 |
| 2025-12-19 | 初始版本创建 | 开发团队 |
| TBD | 补充MeSH集成方案 | - |
| TBD | A/B测试结果分析 | - |

---

**维护说明**: 
- 本文档应随项目演进持续更新
- 所有关键技术决策必须在此记录
- 未解决问题应定期review并优先级排序
